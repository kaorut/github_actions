name: dpkg
on: push
jobs:
  dpkg:
    runs-on: ubuntu-latest
    steps:
    - name: test
      shell: bash
      run: |
        sudo apt-get -y update
        sudo apt-get -y install iwyu
        iwyu -print-resouce-dir 2>/dev/null
        ls -l `iwyu -print-resouce-dir 2>/dev/null`\include
        
